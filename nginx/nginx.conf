server {
    listen 80;
    server_name antiaging-hufs.store;

    # Let's Encrypt 인증서 발급용
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # 발급 완료 후 HTTPS로 리다이렉트 (인증서 발급 후 활성화)
    # location / {
    #     return 301 https://$host$request_uri;
    # }

    # 임시로 HTTP로 서비스 (인증서 발급 후 제거)
    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /app/staticfiles/;
    }
}

# HTTPS 설정 (인증서 발급 후 활성화)
# server {
#     listen 443 ssl;
#     server_name antiaging-hufs.store;
#
#     ssl_certificate /etc/letsencrypt/live/antiaging-hufs.store/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/antiaging-hufs.store/privkey.pem;
#
#     location / {
#         proxy_pass http://web:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /static/ {
#         alias /app/staticfiles/;
#     }
# }
